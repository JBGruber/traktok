% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_hidden.r
\name{tt_slideshow_hidden}
\alias{tt_slideshow_hidden}
\title{Get slideshow metadata, images and music from URLs}
\usage{
tt_slideshow_hidden(
  slideshow_urls,
  save_images = TRUE,
  save_music = TRUE,
  overwrite = FALSE,
  dir = ".",
  solve_captchas = FALSE,
  timeout = 30L,
  sleep_pool = 1:10,
  cookiefile = NULL,
  verbose = interactive(),
  headless = TRUE
)
}
\arguments{
\item{slideshow_urls}{vector of URLs or IDs to TikTok slideshows (photo
posts).}

\item{save_images}{logical. Should the images be downloaded.}

\item{save_music}{logical. Should the music of the slideshows be downloaded
(as mp3).}

\item{overwrite}{logical. If save_video=TRUE and the file already exists,
should it be overwritten?}

\item{dir}{directory to save videos files to.}

\item{solve_captchas}{open browser to solve appearing captchas manually.}

\item{timeout}{maximum time (in seconds) to wait for a post to load.}

\item{sleep_pool}{a vector of numbers from which a waiting period is randomly
drawn.}

\item{cookiefile}{path to your cookiefile. Usually not needed after running
\link{auth_hidden} once. See \code{vignette("unofficial-api", package =
"traktok")} for more information on authentication.}

\item{verbose}{should the function print status updates to the screen?}

\item{headless}{should the browser window stay hidden?}
}
\value{
a data.frame with the same columns as \link{tt_videos_hidden}.
}
\description{
\ifelse{html}{\figure{api-unofficial.svg}{options: alt='[Works on:
  Unofficial API]'}}{\strong{[Works on: Unofficial API]}}

TikTok does not include the data of slideshows (photo posts) in the page
source that \link{tt_videos_hidden} relies on. This function instead opens
each post in a (headless) browser, waits until the site has requested the
post data itself and collects metadata, images and music from there. This
is considerably slower than \link{tt_videos_hidden} and needs the
\code{chromote} package (and a Chrome or Chromium browser) installed.
}
\details{
Images are saved as \code{<author>_video_<id>_<n>.jpeg} and the
music as \code{<author>_video_<id>.mp3} in \code{dir}. The paths are
returned in the columns \code{video_fn} (comma-separated) and
\code{music_fn}. Metadata about the music is in the \code{music} column.
}
\examples{
\dontrun{
tt_slideshow_hidden(
  "https://www.tiktok.com/@chriskuvacyt/photo/7560013468020034848"
)
}
}
